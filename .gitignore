node_modules/
/dist/
*.log

*.tsbuildinfo

# Робочі дані сервера
.indesign-mcp/

# Резервні копії документів InDesign
_backups/

# InDesign
*.idlk

.DS_Store

# Робочий каталог subagent-driven-development (не для комміту)
.superpowers/

# Виміри прогонів — 8+ МБ на прогін. Лишаються на диску поруч зі звітом,
# у git не їдуть: у нову сесію їх дає ШЛЯХ, а не історія.
docs/прогони/*.measurements.json

# Translation tooling (working aid, not part of the server)
.i18n/

# Робочі артефакти агента й SDD-процесу — не частина продукту
.claude/
.superpowers/

# ПРИВАТНЕ — НІКОЛИ НЕ КОМІТИТЬСЯ.
# Усе, що називає конкретне видання чи людину: справжні конфіги видань,
# шляхи до документів, назви книжок, імена, ISBN, штрихкоди, вичитки,
# редакційні рішення. Інструмент розрахований на БАГАТО книжок, тож нічого
# з цього не має бути в його коді. У configs/ лежать лише синтетичні приклади.
private/
